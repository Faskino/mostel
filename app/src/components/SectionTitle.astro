---
const {text, type='title'} = Astro.props
const { class: className} = Astro.props;
---

{type === 'title' ? (
  <h1 class={`text-2xl md:text-5xl font-bold font-nunito  ${className}`}>{text}</h1>
) : (
  <h2 class={` text-base md:text-3xl text-white  ${className}`}>{text}</h2>
)}